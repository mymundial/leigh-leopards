{
  "name": "leigh-leopards-matchday",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "firebase:login": "npx firebase-tools login",
    "firebase:deploy": "npx firebase-tools deploy --only firestore,storage --project leigh-leopards",
    "firebase:emulators": "npx firebase-tools emulators:start --only auth,firestore,storage --project leigh-leopards"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
